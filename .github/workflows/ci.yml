name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: ./configure --enable-warnings
    - name: make
      run: make -j$(nproc)
    - name: make install
      run: make install
